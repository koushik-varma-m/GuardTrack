name: guardtrack_guard_app
description: Guard mobile app for Guard Monitoring System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # HTTP & Storage
  http: ^1.1.0
  shared_preferences: ^2.2.2
  
  # QR Scanner
  mobile_scanner: ^5.2.3
  
  # Date Formatting
  intl: ^0.19.0
  
  # Material 3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

